[build]
builder = "NIXPACKS"

[variables]
# Indispensable para que el servidor web sepa dónde entrar
DOCUMENT_ROOT = "/public"

[deploy]
# Solo comandos de preparación. Nixpacks iniciará el servidor web automáticamente.
startCommand = "php artisan migrate --force && php artisan optimize"

[deploy.workers]
# Proceso independiente para Reverb
reverb = "php artisan reverb:start --host=0.0.0.0 --port=8080"